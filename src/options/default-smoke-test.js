/**
 * Verify that system works well under minimal traffics that gives us a baseline
*/

/**
 * Verify code doesnt have any errors
 * Verify that system doenst throw any errors
 */

export const options ={
    vus :3, //max is 5 VUs
    durations: '1m'
 }